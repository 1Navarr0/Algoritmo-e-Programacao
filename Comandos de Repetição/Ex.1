algoritmo "01 - Repetição"

var
    numero : inteiro
    c : inteiro
    soma : inteiro

inicio
   
    escreval("Informe um número: ")
    leia(numero)

    enquanto numero > 0 faca
        numero <- numero - 1
        soma <- soma + numero

    fimenquanto
    escreva(soma)

fimalgoritmo
